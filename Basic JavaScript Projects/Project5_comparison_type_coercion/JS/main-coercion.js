// Define a number variable
var numberVariable = 42;

// Define a string variable
var stringVariable = "The number is: ";

// Concatenate the number with the string (type coercion will occur)
document.write(stringVariable + numberVariable);