// JS/main.js

// Using Boolean logic to compare numbers
var comparison1 = 10 > 2; // True because 10 is greater than 2
var comparison2 = 5 < 1;  // False because 5 is not less than 1

// Output the results
document.getElementById("comparison1").innerHTML = "Comparison 1: " + comparison1;
document.getElementById("comparison2").innerHTML = "Comparison 2: " + comparison2;
